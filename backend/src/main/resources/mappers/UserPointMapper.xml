<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.farmday.review.mapper.UserPointMapper">

    <!-- USER_POINT 이력 저장 -->
    <insert id="insertUserPoint" parameterType="map">
        INSERT INTO USER_POINT (
            point_history_id,
            user_no,
            point_amount,
            point_type,
            related_id,
            description,
            created_date
        ) VALUES (
            SEQ_USER_POINT.NEXTVAL,
            #{userNo},
            #{pointAmount},
            #{pointType},
            #{relatedId},
            #{description},
            SYSDATE
        )
    </insert>

    <!-- USERS 테이블 포인트 누적 증가 -->
    <update id="updateUserPoint" parameterType="map">
        UPDATE users
        SET points = points + #{pointAmount}
        WHERE user_no = #{userNo}
    </update>

</mapper>